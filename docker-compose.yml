version: "3.9"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: warmup
      POSTGRES_PASSWORD: warmup
      POSTGRES_DB: warmup
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
